paths:
  /products/view:
    get:
      summary: View a product
      tags:
        - Product
      parameters:
        - name: productId
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Product details retrieved successfully
